import React from "react"
import Alert from 'react-bootstrap/lib/Alert'

export default class About extends React.Component {
  render() {
    return (
      <Alert bsStyle="info">
        <strong>About us page here...</strong>
      </Alert>
    )
  }
}
