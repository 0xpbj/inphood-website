var React = require('react')
import Alert from 'react-bootstrap/lib/Alert'

export default class About extends React.Component {
  render() {
    return (
      <div>
        <Alert bsStyle="info">
          <strong>About page loading...</strong>
        </Alert>
      </div>
    )
  }
}
