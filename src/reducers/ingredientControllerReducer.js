import {} from '../constants/ActionTypes'

const initialState = {
}

export default function ingredientControllerReducerLogic(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
